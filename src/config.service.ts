import type { ParsedCommands } from "./protocol/config.commands.js";

class ConfigService {
  static handleTask(task: ParsedCommands) {
    const service = new ConfigService();
  }
}

export default ConfigService;
